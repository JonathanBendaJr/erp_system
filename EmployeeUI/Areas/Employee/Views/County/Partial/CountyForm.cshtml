

@model DTO.CountyDTO
<div class="box-body">
    <div class="form-group">
        <label>County</label>
        @Html.TextBoxFor(x => x.CountyName, new { @class = "form-control", @placeholder = "Enter County" })
        @Html.ValidationMessageFor(x => x.CountyName, "", new { @class = "text-danger" })
    </div>
    <input hidden type="number" value="@Model.ID" name="ID" />
    <div class="card-footer">
        <button type="submit" class="btn btn-primary">Save</button>
    </div>


</div>

